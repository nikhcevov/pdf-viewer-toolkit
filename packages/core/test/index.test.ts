import { afterEach, describe, it, jest } from '@jest/globals';

afterEach(() => {
  jest.clearAllMocks();
});

describe('PDFViewer', () => {
  it('should create a PDF viewer', async () => {
    // TODO: tests
  });
});
